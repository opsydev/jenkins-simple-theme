document.observe("dom:loaded", function() {
  //Get rid of stupid extra nbsp in side panel
  $$('#side-panel #navigation #tasks .task a:first-child').each(function(node){
    node.
    node.remove();
  });
});